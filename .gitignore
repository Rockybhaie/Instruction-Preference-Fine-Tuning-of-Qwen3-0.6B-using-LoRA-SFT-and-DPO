# ============================================================
#  Large model artifacts - regenerated by the notebooks.
#  DO NOT commit these (GitHub rejects files >100 MB).
# ============================================================

# all LoRA adapters + the merged SFT model weights
*.safetensors
# training_args.bin / optimizer states
*.bin
# best_sft_adapter.zip and any other archives
*.zip
# 11 MB tokenizer copy duplicated in every trial folder
tokenizer.json
# the ~1.19 GB merged SFT model (way over GitHub's limit)
**/sft_merged/
# Kaggle scratch directory
**/.virtual_documents/

# ============================================================
#  Python / OS noise
# ============================================================
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
Thumbs.db
